{
  "name": "vocoder-sdk",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "release": "changeset publish",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "@changesets/cli": "^2.26.2",
    "typescript": "^5.4.0",
    "prettier": "^3.0.0"
  }
}
